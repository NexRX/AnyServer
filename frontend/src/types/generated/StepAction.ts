// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { ArchiveFormat } from "./ArchiveFormat";
import type { FileOperation } from "./FileOperation";
import type { StopSignal } from "./StopSignal";
import type { VersionPick } from "./VersionPick";

export type StepAction = { "type": "download", url: string, destination: string, filename: string | null, executable: boolean, } | { "type": "extract", source: string, destination: string | null, format: ArchiveFormat, } | { "type": "move", source: string, destination: string, } | { "type": "copy", source: string, destination: string, recursive: boolean, } | { "type": "delete", path: string, recursive: boolean, } | { "type": "create_dir", path: string, } | { "type": "run_command", command: string, args: Array<string>, working_dir: string | null, env: { [key: string]: string }, } | { "type": "write_file", path: string, content: string, } | { "type": "edit_file", path: string, operation: FileOperation, } | { "type": "set_permissions", path: string, mode: string, } | { "type": "glob", pattern: string, destination: string, } | { "type": "set_env", variables: { [key: string]: string }, } | { "type": "set_working_dir", path: string, } | { "type": "set_stop_command", command: string, } | { "type": "set_stop_signal", signal: StopSignal, } | { "type": "send_input", text: string, } | { "type": "send_signal", signal: StopSignal, } | { "type": "sleep", seconds: number, } | { "type": "wait_for_output", 
/**
 * The text to search for in console output (case-insensitive substring match).
 */
pattern: string, 
/**
 * Maximum seconds to wait before giving up and continuing.
 */
timeout_secs: number, } | { "type": "resolve_variable", 
/**
 * The URL to GET.  Supports `${param}` variable substitution.
 */
url: string, 
/**
 * Dot-separated path to navigate the JSON response (e.g.
 * `"builds"` or `"data.latest"`).  If omitted the root is used.
 */
path: string | null, 
/**
 * When the resolved value is an array, which element to pick.
 * Defaults to `Last`.
 */
pick: VersionPick, 
/**
 * If the picked element is an object, which key to extract as the
 * value.  If omitted, the element must be a string or number.
 */
value_key: string | null, 
/**
 * The variable name to store the result in.  Subsequent steps can
 * reference it as `${variable}`.
 */
variable: string, } | { "type": "download_github_release_asset", 
/**
 * The name of a `github_release_tag` parameter to use.
 * This must reference an actual parameter of type `GithubReleaseTag`
 * in the template's parameter list.
 */
tag_param: string, 
/**
 * Asset filename matcher. Can be an exact filename or a regex pattern.
 * Regex patterns must be wrapped in forward slashes (e.g. `/pattern/`).
 */
asset_matcher: string, 
/**
 * Destination directory to save the downloaded asset.
 */
destination: string, 
/**
 * Optional filename to save as. If not specified, uses the original asset name.
 */
filename: string | null, 
/**
 * Whether to mark the downloaded file as executable (Unix only).
 */
executable: boolean, } | { "type": "download_curse_forge_file", 
/**
 * The name of a `CurseForgeFileVersion` parameter.
 * Must reference a parameter of type `CurseForgeFileVersion` in the
 * template's parameter list.
 */
file_param: string, 
/**
 * Destination directory to save the downloaded file.
 */
destination: string, 
/**
 * Optional filename override. If not specified, uses the server
 * pack's original filename from CurseForge.
 */
filename: string | null, 
/**
 * Whether to mark the downloaded file as executable (Unix only).
 */
executable: boolean, } | { "type": "steam_cmd_install", 
/**
 * Override the Steam app ID. If `None`, uses the server config's
 * `steam_app_id`.
 */
app_id: number | null, 
/**
 * Whether to log in anonymously.  Most dedicated servers allow
 * anonymous login.  Defaults to `true`.
 */
anonymous: boolean, 
/**
 * Extra arguments appended to the SteamCMD command line before
 * `+quit`.  Useful for beta branch selection, etc.
 */
extra_args: Array<string>, } | { "type": "steam_cmd_update", 
/**
 * Override the Steam app ID. If `None`, uses the server config's
 * `steam_app_id`.
 */
app_id: number | null, 
/**
 * Whether to log in anonymously.  Defaults to `true`.
 */
anonymous: boolean, 
/**
 * Extra arguments appended to the SteamCMD command line before
 * `+quit`.
 */
extra_args: Array<string>, };
